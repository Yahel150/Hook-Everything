# Color DLL Project

Visual Studio DLL project for the Calculator text-color hook. Builds dllmain.cpp, a separate copy of ../Color.cpp, with pch.h and pch.cpp.

Use Win32 with the configured v145 toolset and Windows SDK 10.0. Rebuild the DLL to apply source changes.
