# Reverse Calculator Text

reverseText.cpp hooks SetWindowTextW and reverses its text before calling the original function. Includes Calculator, an injector, and a prebuilt hook DLL.

Uses a hardcoded IAT offset for a specific 32-bit executable. No complete build project is included.
